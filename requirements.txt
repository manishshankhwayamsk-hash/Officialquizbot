# Core Telegram bot framework (async). [job-queue] extra is required because the
# bot uses app.job_queue.run_repeating() for the automatic 6-hourly DB backup.
python-telegram-bot[job-queue]>=22.0,<23.0

# HTTP calls to AI providers (Groq/Gemini/OpenAI/OpenRouter/self-hosted) and misc APIs.
requests>=2.31.0

# Postgres driver — used automatically when DATABASE_URL is set (recommended on
# Render, since Render's local disk is wiped on every redeploy; SQLite alone would
# lose all data). If DATABASE_URL is left empty the bot falls back to local SQLite.
psycopg2-binary>=2.9.9

# Loads a local .env file when running/testing outside Render (Render itself injects
# env vars directly, so this is mainly for your own PC).
python-dotenv>=1.0.1

# Anthropic (Claude) API client — one of the supported AI providers for MCQ generation.
anthropic>=0.100.0

# Used by /youtube to pull a video's transcript before generating MCQs from it.
youtube-transcript-api>=0.6.2

# Used by /pdf to extract text from uploaded PDFs.
pypdf>=4.2.0
